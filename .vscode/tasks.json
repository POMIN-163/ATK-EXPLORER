{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": ["git-bash"],
      "args": [
        "-c",
        "echo build.... && make -j8 all && start Alacritty.exe --command openocd"
      ]
    }
  ]
}
